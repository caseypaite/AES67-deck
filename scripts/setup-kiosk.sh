#!/bin/bash
echo "Setting up Wayland kiosk mode for AES67-Deck UI..."
# Assuming Cage is installed and we are launching Chromium in kiosk mode
echo "To run UI in kiosk mode: cage -- chromium-browser --kiosk http://localhost:3000"
