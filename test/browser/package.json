{
  "name": "aes67-deck-browser-test",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Headless-browser smoke test for the TIMELINE view (cue list + loudness log)",
  "scripts": {
    "test": "bash run.sh",
    "showcase": "bash showcase.sh",
    "postinstall": "node ./node_modules/puppeteer/install.mjs || true"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "puppeteer": "^25.9.0",
    "yauzl": "^3.4.0"
  }
}
