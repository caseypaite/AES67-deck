# AES67-Deck: realtime scheduling + memory locking for the audio group.
# pam_limits applies these to members of "audio" on login / systemd --user.
@audio   -  rtprio     95
@audio   -  memlock    unlimited
@audio   -  nice      -19
